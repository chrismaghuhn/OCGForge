#pragma once

namespace ygo::teacher {

// Task 8 owns the complete value and canonical codec. Task 3 intentionally
// exposes only the forward declaration; it does not carry explanation data.
struct TeacherDecisionExplanation;

}  // namespace ygo::teacher
