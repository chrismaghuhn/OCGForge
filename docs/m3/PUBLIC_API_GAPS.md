# M3 Public API Gaps

| Gap | Status | Scope | Public API evidence | Impact | Privacy | M3.5 action |
| --- | --- | --- | --- | --- | --- | --- |
| INDIVIDUAL_XYZ_MATERIAL_QUERY | RESOLVED_BY_REPOSITORY_PATCHSET | pinned ocgcore public API 11.0 plus the OCGForge derived patchset | OCG_DuelQuery, OCG_DuelQueryLocation, OCG_DuelQueryFieldCard: The unmodified base checkout reproduced the empty overlay query; repository patch 0001 fixes only the existing parent-location plus overlay_seq lookup, and m35_xyz_material_query_test plus m2_1_xyz_api_test prove the real public query and privacy projections. | Canonical M3.5 observations expose visible material identity through the existing overlay_seq contract and continue to redact opponent face-down material identity. | Identity is exposed only after the same parent visibility gate used for the visible Xyz card; hidden paired-world material identities remain redacted and hash-equal. | retain the patch as an ordered repository input and prepare an upstream-compatible change if accepted; until then the base cached core remains immutable |
| FIXTURE_RUNNER_PUBLIC_SETUP_SCOPE | RECORDED_FOR_M3_5 | OCGForge test infrastructure, not an upstream ocgcore gap | CoreHost::load_fixture_script, CoreHost::load_fixture_card: Current fixture setup is intentionally script-backed; no general board-construction API is required by the closed fixtures and no missing ocgcore capability has been proven. | M3 does not add a general public board-construction API or private core access. | Any future setup projection must remain test-only and must not widen runtime hidden-information access. | evaluate a descriptor-backed fixture setup projection if more coverage requires it |
| START_PLAYER_SELECTION_CONTROL | RESOLVED_BY_REPOSITORY_PATCHSET | pinned ocgcore public API 11.0 plus the OCGForge derived patchset | OCG_DuelOptions, OCG_StartDuel, OCG_DuelQueryFieldCard: The unmodified base API had no starting-player setter and always initialized player 0; repository patch 0002 adds only OCG_DuelSetStartingPlayer before duel start, preserves default 0, rejects invalid values and post-start calls, and is proven by m35_starting_player_api_test plus canonical full-game partitions. | Canonical M3.5 runners can deterministically exercise starting-player 0 and 1 while retaining the same default behavior and deck-seat semantics. | No hidden card information is exposed; the patch changes only pre-duel turn-player selection and acceptance coverage. | retain the additive setter as an ordered repository input and prepare an upstream-compatible change with regression tests if accepted |

The immutable base ocgcore checkout and all upstream component sources remain unchanged; canonical execution uses the ordered derived repository patchset.
